{
    "$schema": "http://json-schema.org/draft-06/schema#",
    "description": "Availability model",
    "type": "object",
    "properties": {
        "IsAvailable": { "type": "boolean" },
        "AppointmentDate": { "type": "date" }
    },
    "required": ["IsAvailable", "AppointmentDate"]
}
